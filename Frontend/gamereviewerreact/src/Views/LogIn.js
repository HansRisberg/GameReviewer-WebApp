import LoginForm from "../Components/LoginForm"

const LoginView = () => {
    return(
        <div>
            <h1>Login View</h1>
            <p>This is where you can log in</p>
            <LoginForm />
        </div>
    );
};

export default LoginView;